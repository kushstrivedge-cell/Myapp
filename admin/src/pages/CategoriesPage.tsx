import ResourcePage from '../components/ResourcePage';type Row={id:string;name:string;slug:string;parentId:string|null;_count:{products:number}};export default function CategoriesPage(){return <ResourcePage<Row> endpoint="/admin/categories" searchText={r=>`${r.name} ${r.slug}`} columns={[{key:'name',header:'Category',render:r=>r.name},{key:'slug',header:'Slug',render:r=>r.slug},{key:'parent',header:'Parent',render:r=>r.parentId??'Top level'},{key:'products',header:'Products',render:r=>r._count.products}]}/>}
